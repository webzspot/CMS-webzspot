# Direct backend URL. Needs CORS enabled on the backend for http://localhost:5173.
VITE_API_BASE_URL=https://cms-backend-nw6j.onrender.com/api/v1

# Fallback while the backend has no CORS: use the Vite proxy in vite.config.js
# VITE_API_BASE_URL=/api/v1
